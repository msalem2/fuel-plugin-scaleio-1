name: scaleio-sds
meta:
  name: "ScaleIO SDS"
  description: "ScaleIO SDS Storage node"
  has_primary: false                 # whether has primary role or not
  public_ip_required: false         # whether requires public net or not
  weight: 103                       # weight that will be used for ordering on fuel ui
volumes_roles_mapping:
- allocate_size: min
  id: os
- allocate_size: all
  id: image
